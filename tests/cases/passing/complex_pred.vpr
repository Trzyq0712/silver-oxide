field f: Int
field g: Int

predicate complex(this: Ref, that: Ref, cond: Bool) {
  acc(this.f, 1/1) &&
  (cond ==> acc(that.f, !cond ? 1/2 : 1/3)) &&
  (!cond ? acc(that.f, 1/2) : (acc(that.g, 1/2) && that.g > 0))
}
