field value: Int
field next: Ref

predicate list(x: Ref) {
  acc(x.value, write) && acc(x.next, write) && (x.next != null ==> list(x.next))
}

function sum(x: Ref): Int
  requires list(x)
{
  unfolding list(x) in x.value + (x.next == null ? 0 : sum(x.next))
}
