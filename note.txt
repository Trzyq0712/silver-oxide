Let's try to fix the missing cases that do not verify of tha @../cases/rust/structs_enums.vpr . We have already derived some of the minimal examples for which we are unable to verify. One of the ones that I
  have investigated personally works on the minimized example. More concretely I have made it work for exhaustiveness @../cases/rust/minimal/exhaust_with_axiom_int.vpr . Please verify that we now cover the
  this case in the actual big file. Then proceed with other cases. At every point make sure that we don't completely destroy the performance on the things we already verify. For this purpose derive a base
  test that we are fully verifying (s.t. we are not affected by early crashing). Use that test case to measure if the performance does not deteriorate. Good luck!


   We would like now to focus on cases where we struggle with performance. We'd like to establish a set of features we currently support. For once we currently cannot verify loops or
  reborrowing (i.e. returning a mut ref). We should test out other spec-less programs in rust to try to figure out how well we do with the verification and which things are the
  bottlenecks. To this end we'd like to generate some rust programs. We want to do it systematically, testing out varios combinations of features, structs, enums, and controlflow. One
  thing we should try is rust programs with generics and/or traits. We should use prusti to generate the viper encoding. (Prusti is in one of the ancestor dirs, maybe youo can copy-in the
  binary here.) The goal is to never have to go to tier 4 for verification. We should establish the cases for which this happens.

Sharp signal. Two concrete redundancies in shape_grow:

1. Same &mut ref snapshotted 100+ times. 112 p_Ref_mutable_snap#requires calls, only 4 distinct refs — one deref'd 62×, another 29×, another 12×. The ref pointer is block-invariant (only its target heap changes), but each block re-extracts snap[hN] → cons.0 → Ref fresh. This churn feeds the whole proj/inj long tail.

2. 83 literal !true dead arms (of 367 true pcs). Let me confirm they're genuinely static-dead:

  Ran 1 shell command

Confirmed — bb13 <e83, !true> is an empty, statically-dead block (literal-false pc), and 82 more like it.

So the remaining shape_grow cost is block explosion / frontend redundancy, not any verifier rule. Ranked ideas:

1. CSE the block-invariant &mut ref→address extraction (frontend) — highest leverage

The same ref is snapshotted 62× / 29× / 12× (4 distinct refs, 112 calls). The ref pointer and its target address (s_Ref_mutable_cons.0(ref_snap)) are invariant across the arm — only the pointed-to heap changes. Each block re-emits snap[hN] → cons.0 → Ref fresh → 100+ distinct nodes the verifier re-reduces (this is the entire proj-/inj- long tail at 2-4ms each). Snapshot the ref once at borrow, reuse the address ⇒ ~100→4. Mechanism = the T2 cube-CSE / Sink::emit_pure value-numbering ([[project_perm_collapse_root_cause]]) extended to ref derefs. Medium effort, frontend-only.

2. Prune literal-dead !true arms (frontend) — cheap, needs confirmation

83 empty !true blocks + their joins. A cube with a literal-false conjunct is unreachable → skip emission, short-circuit the join to the live arm ([[project_reach_dnf_pc]] already threads reach cubes; just constant-fold !true→false). Caveat: the empty block itself is near-free; the win only lands if pruning also drops the forced merge — I'd verify that before committing. Low effort.

3. Chunk-keyed snap resolution (verifier) — architectural

153 snap[h] recomputed over a growing heap. If snap reused its node when the resolved chunk is unchanged (not the whole-heap identity), all the stable-chunk snaps collapse. This is the ghost-resident/block-local heap model ([[project_block_heap_consume_model]], [[project_ghost_local_egraphs]]). Big, but kills the O(n²) snap smell file-wide, not just shape_grow.

4. Structural join collapse (block-VMIR) — biggest

Removes the heterogeneous variant tower itself at the join, so there's nothing to snapshot/project through. The real fix; whole-program effort ([[project_heap_ternary_join_select]]).

Recommendation: spike #1 — unambiguous redundancy (same ref, same address, 62×), frontend-isolated, and it attacks the long tail that now dominates. Want me to prototype it and measure?





 Here's the full chain, from Rust down to the two chunks.

  The Rust

  world_kick_slowest, innermost else (physics_step.rs:171):

  } else {
      body_apply_impulse(&mut w.c, j);   // which != 0, which != 1
  }

  MIR splits &mut w.c into two borrows

  Because the callee takes &mut Body, MIR makes a borrow and a reborrow:

  bb18[4]:  _38 = &mut ((*_1).c)
  bb18[5]:  _37 = &mut (*_38)

  Viper: three ref cells, one location

  Prusti gives each borrow a ref cell and an erase/unerase round trip. Verbatim from physics_step.vpr (bb18):

  viper
  // _38 = &mut (*_1).c
  p_Ref_mutable_assign(_38p, s_Body_type(),
      p_Ref_mutable_arbitrary_value(s_World_field_2(p_Ref_mutable_snap(_1p, s_World_type()).s_Ref_mutable_0), ...))
  make_generic_Body(p_Ref_mutable_snap(_38p, s_Body_type()).s_Ref_mutable_0)     // ← consume #1

  // _37 = &mut (*_38)
  if (_from_bb12_to_bb15 && _from_bb15_to_bb18) {
      make_concrete_Body(p_Ref_mutable_snap(_38p, s_Body_type()).s_Ref_mutable_0)
  }
  p_Ref_mutable_assign(_37p, s_Body_type(),
      p_Ref_mutable_arbitrary_value(p_Ref_mutable_snap(_38p, s_Body_type()).s_Ref_mutable_0, ...))
  make_generic_Body(p_Ref_mutable_snap(_37p, s_Body_type()).s_Ref_mutable_0)     // ← consume #2, FAILED

  The two relevant contracts:

  viper
  method make_generic_Body(self: Ref)
    requires acc(p_Body(self), write)                    // consumes p_Body
    ensures  acc(p_Param(self, s_Body_type()), write)    // produces p_Param

  method make_concrete_Body(self: Ref)
    requires acc(p_Param(self, s_Body_type()), write)    // consumes p_Param
    ensures  acc(p_Body(self), write)                    // produces p_Body — a NEW chunk

  Why the addresses miss on ground

  Each ref cell's target comes from two method postconditions, not from syntax:

  viper
  method p_Ref_mutable_assign(self, U$1, value)
    ensures p_Ref_mutable_snap(self, U$1) == value       // an equation on a snap function

  function p_Ref_mutable_arbitrary_value(r, metadata): s_Ref_mutable
    ensures result.s_Ref_mutable_0 == r

  So deref(_37p) == deref(_38p) == w.c is derived, and it's inhaled inside the arm — pc-guarded, not a ground e-class equality. Syntactically the three address terms are unrelated:

  ┌────────┬─────────────────────────────────────┬──────────────────────────────────────┐
  │ chunk  │            address term             │                origin                │
  ├────────┼─────────────────────────────────────┼──────────────────────────────────────┤
  │ 147    │ p_Body(s_World_field_2(deref(_1p))) │ unfold p_World at the top of the arm │
  ├────────┼─────────────────────────────────────┼──────────────────────────────────────┤
  │ 3963   │ p_Body(deref(_38p))                 │ make_concrete_Body's ensures         │
  ├────────┼─────────────────────────────────────┼──────────────────────────────────────┤
  │ demand │ p_Body(deref(_37p))                 │ consume #2                           │
  └────────┴─────────────────────────────────────┴──────────────────────────────────────┘

  That ground miss is expected — resolving it under the pc is the mut_reborrow_call_in_branch fix (0f1aba4).

  The two-step failure

  Consume #1 demands p_Body(deref(_38p)). Ground miss → pc probe → matches 147 (w.c). Correct. But the debit is pc-gated (it must be — off-path the addresses are unrelated and nothing was given up), so 147 doesn't disappear. It survives holding:

  1/1 − ite(e316, ite(e329, 1/1, 0/1), 0/1)

  where e316 = s_Bool_value(which==0) == false, e329 = s_Bool_value(which==1) == false — the block pc, i.e. the else/else arm. Zero on-path, 1/1 off-path.

  make_concrete_Body then mints a fresh, full p_Body chunk 3963 at deref(_38p).

  Consume #2 demands p_Body(deref(_37p)). Ground miss again. Under the pc it aliases both 147 and 3963. The old code called chunk_under_pc, which returns the first match — 147, the spent one, holding 0 on-path. Insufficient permission.

  [pc-match] demanded=3963 matched=147 held(3): [134, 141, 147]
  [pc-match] demanded=4437 matched=147 held(4): [134, 141, 147, 3963]
                                    ^^^ drained above; 3963 has the 1/1

  Order is insertion order, and 147 came from the unfold p_World long before the round trip — so the spent alias always sorts ahead of the live one.

  The general pattern

  Any &mut-reborrow chain inside a branch arm. Each link is assign + make_generic (consume) and make_concrete (produce), so every link leaves a spent, pc-gated chunk behind at a different syntactic address for the same location while the live permission moves to the newest one. One link works; two or more break under a head-taking lookup.

  The fix sums the whole alias set — (1/1 − ite(pc,1/1,0)) + 1/1 ≥ 1/1 under the pc — which is what the demanded-chunk-exists path (invariant 7) already did. The miss path just hadn't been generalized to match.

  ----------

  So if I understand corrctly, the chunks are initially completely separate cause the locations are not the same on the syntax level. The locations are, however, equal semantically, which is not immediately noted down by our verifier. Then when we look for a chunk, we match the old one, but not the new one. Correct?

Yes, with two refinements.

Correct: addresses syntactically distinct (p_Body(s_World_field_2(deref(_1p))) vs p_Body(deref(_38p)) vs p_Body(deref(_37p))). Equality is derivable — but only from inhaled method postconditions inside the arm, so it's pc-guarded, not a ground e-class merge. Ground lookup misses.

Refinement 1: verifier does eventually note the equality. chunk_under_pc finds it. Not a knowledge gap — a selection gap. Probe returned first match; both 147 and 3963 are valid aliases under the pc.

Refinement 2: "matched old, not new" understates it. Both matched. Bug = taking one instead of summing. Perm is split across the alias set:

147:  1/1 − ite(pc, 1/1, 0/1)    # 0 on-path
3963: 1/1                        # the live one

Neither alone satisfies the demand off-path + on-path simultaneously. 147 is 0 on-path; 3963 doesn't exist off-path. Only sum works: (1/1 − ite(pc,1/1,0)) + 1/1 ≥ 1/1 holds both ways.

Ordering made it deterministic-bad: insertion order, 147 from unfold p_World at arm top, always ahead of 3963.

So: not "picked wrong chunk" → "picked a chunk when it needed all of them". Fix (894a300) sums the set, matching what the ground-hit path (invariant 7) already did.

✻ Cogitated for 12s

❯ So it is also the case that we do not materialize the sum expression in the egraph, right?

Let me check rather than assert from memory.

  Ran 2 shell commands

Partly — depends which "sum".

Materialized: the proof term. declaration.rs:1284:

total = ctx.add(Symbolic::Binary(BinOp::AddR, [total, p]));

That's a real e-node. It has to be — prove_under_pc(sufficient, pc_lits) at :1290 needs !(total < needed) as a graph term to rewrite on. Same for the dump_term output.

Not materialized: anything persistent.

- No consolidated chunk. Heap keeps the alias set separate; debit is distributed greedily per-chunk (for chunk in set, :1319), each getting its own min(hold, remaining) gated by pc.
- No ground union of addresses. Deliberate — commit message: "ground never consolidates chunks that are only conditionally equal." Off-path deref(_37p) != deref(_38p) and merging would fabricate aliasing.
- Value agreement likewise stays guarded: assume_guarded(agree, pc) at :1305, not a union.

So the sum is a throwaway proof scaffold, not a heap representation change. After the consume the heap still holds N chunks at N syntactically-distinct addresses; the invariant that survives is per-chunk ≥ 0 plus sum-≥ 0, exactly as [[project_block_heap_consume_model]] has it.

The transient node does stick around in the e-graph as garbage though — nothing prunes it. Relevant to [[project_true_class_accretion]] if the alias sets get large, but N here is 2–3.

✻ Cogitated for 20s

※ recap: Goal: understand the pc-alias consume bug in the verifier. Just confirmed the alias sum is materialized only as a throwaway proof term, not as a merged chunk or ground union. No pending action; waiting on your next question. (disable recaps in /config)
