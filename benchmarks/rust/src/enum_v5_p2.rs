//! GENERATED by benchmarks/rust/gen_enum.py -- do not edit by hand.
//!
//! 5 payload variants, payload nesting depth 2. Spec-less Rust; obligations come
//! from Prusti's type predicates. No loops, no recursion, no returned references.
//!
//! `e_grow` writes through a `&mut` payload once per arm -- the shape that makes the
//! conditional footprint's gate undecidable from the path condition.

pub struct L0 {
    pub x: i32,
    pub y: i32,
}

pub struct L1 {
    pub a: L0,
    pub b: L0,
}

pub enum E {
    V0(L1),
    V1(L1),
    V2(L1),
    V3(L1),
    V4(L1),
}

pub fn e_tag(e: &E) -> i32 {
    match e {
        E::V0(_) => 0,
        E::V1(_) => 1,
        E::V2(_) => 2,
        E::V3(_) => 3,
        E::V4(_) => 4,
    }
}
pub fn e_read(e: &E) -> i32 {
    match e {
        E::V0(p) => p.a.x + p.a.y,
        E::V1(p) => p.a.x + p.a.y,
        E::V2(p) => p.a.x + p.a.y,
        E::V3(p) => p.a.x + p.a.y,
        E::V4(p) => p.a.x + p.a.y,
    }
}
pub fn e_grow(e: &mut E, k: i32) {
    match e {
        E::V0(p) => {
            p.a.x = p.a.x * k + 0;
            p.a.y = p.a.y + p.a.x;
        }
        E::V1(p) => {
            p.a.x = p.a.x * k + 1;
            p.a.y = p.a.y + p.a.x;
        }
        E::V2(p) => {
            p.a.x = p.a.x * k + 2;
            p.a.y = p.a.y + p.a.x;
        }
        E::V3(p) => {
            p.a.x = p.a.x * k + 3;
            p.a.y = p.a.y + p.a.x;
        }
        E::V4(p) => {
            p.a.x = p.a.x * k + 4;
            p.a.y = p.a.y + p.a.x;
        }
    }
}
pub fn e_guarded(e: &mut E, k: i32, up: bool) {
    match e {
        E::V0(p) => {
            if up {
                p.a.x = p.a.x + k;
            } else {
                p.a.x = p.a.x - k;
            }
        }
        E::V1(p) => {
            if up {
                p.a.x = p.a.x + k;
            } else {
                p.a.x = p.a.x - k;
            }
        }
        E::V2(p) => {
            if up {
                p.a.x = p.a.x + k;
            } else {
                p.a.x = p.a.x - k;
            }
        }
        E::V3(p) => {
            if up {
                p.a.x = p.a.x + k;
            } else {
                p.a.x = p.a.x - k;
            }
        }
        E::V4(p) => {
            if up {
                p.a.x = p.a.x + k;
            } else {
                p.a.x = p.a.x - k;
            }
        }
    }
}
