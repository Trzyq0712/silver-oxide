//! GENERATED by benchmarks/rust/gen_enum.py -- do not edit by hand.
//!
//! 3 payload variants, payload nesting depth 1. Spec-less Rust; obligations come
//! from Prusti's type predicates. No loops, no recursion, no returned references.
//!
//! `e_grow` writes through a `&mut` payload once per arm -- the shape that makes the
//! conditional footprint's gate undecidable from the path condition.

pub struct L0 {
    pub x: i32,
    pub y: i32,
}

pub enum E {
    V0(L0),
    V1(L0),
    V2(L0),
}

pub fn e_tag(e: &E) -> i32 {
    match e {
        E::V0(_) => 0,
        E::V1(_) => 1,
        E::V2(_) => 2,
    }
}
pub fn e_read(e: &E) -> i32 {
    match e {
        E::V0(p) => p.x + p.y,
        E::V1(p) => p.x + p.y,
        E::V2(p) => p.x + p.y,
    }
}
pub fn e_grow(e: &mut E, k: i32) {
    match e {
        E::V0(p) => {
            p.x = p.x * k + 0;
            p.y = p.y + p.x;
        }
        E::V1(p) => {
            p.x = p.x * k + 1;
            p.y = p.y + p.x;
        }
        E::V2(p) => {
            p.x = p.x * k + 2;
            p.y = p.y + p.x;
        }
    }
}
pub fn e_guarded(e: &mut E, k: i32, up: bool) {
    match e {
        E::V0(p) => {
            if up {
                p.x = p.x + k;
            } else {
                p.x = p.x - k;
            }
        }
        E::V1(p) => {
            if up {
                p.x = p.x + k;
            } else {
                p.x = p.x - k;
            }
        }
        E::V2(p) => {
            if up {
                p.x = p.x + k;
            } else {
                p.x = p.x - k;
            }
        }
    }
}
