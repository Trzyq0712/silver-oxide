# Members of this corpus we do not verify yet. Format: <vpr-stem> <member>
#
# The corpus rule (README) is that *every* member verifies — a failing member stops at
# its failing instruction and has no stable cost, so it silently corrupts any timing
# taken over its file. This file is the explicit, reviewed exception list: check.sh
# fails on a FAIL that is not listed here, and equally on a listed member that now
# passes (a fixed gap must be deleted from this file, not left to rot).
#
# Keep each entry's reason current. An entry with no live reason is a bug report.

# Enum snapshot tower inversion: the OUTERMOST arm.
#
# NOT a permission-model gap. `make_generic_Shape` / `make_concrete_Shape` destroy
# the `p_Shape` chunk and re-inhale a fresh, havocked one; the only link back is
# the snapshot postconditions plus `make_concrete_s_Shape(make_generic_s_Shape(x))
# == x`, which yields `p_Shape_snap_old == p_Shape_snap_new`. `p_Shape_snap`'s body
# is a 5-deep ternary of `s_Shape_k_cons(..)` arms, so recovering `discr == cons(k)`
# means inverting that tower.
#
# Two of the three pieces now work, and both have their own passing case:
#   * `distinguishing-observation` pins each `snap == s_Int_isize_cons(k)` guard.
#     `s_Int_isize` is a *domain* with a `cons`/`value` retraction, not an `adt`, so
#     two `cons` applications have no free-constructor distinctness; the rule
#     disproves the equality from `value(cons(1)) != value(cons(4))` instead
#     (passing/domain_cons_disequality_via_observation.vpr).
#   * `ite-reduce`'s constructor arm-differs pinning telescopes the tower from the
#     bottom (passing/predicates/domain_cons_tower_inversion.vpr).
#
# What is left is the tower's outermost arm. With the class pinned to
# `Ctor(s_Shape_4_cons)`, the then-arm is the same constructor (no pin, correctly)
# and the else-arm is an undecided nested `ite` with `Unknown` analysis data, so
# nothing pins the guard. MEASURED 2026-08-14: asserting the discriminant equality
# by hand in every arm makes arms 0-3 verify and leaves exactly the arm-4 assert
# failing. Minimal shape and the two candidate representations (a
# set-of-constructors lattice value, or a bounded recursive `arm_differs` through
# undecided arms) are in known_limitations/tower_inversion_outermost_arm.vpr.
#
# Earlier readings of these two, all now refuted: (1) filed with `mat3_mul` as a
# `&mut`-reborrow shape — that one was fixed by making `Chunk::guard` load-bearing
# and these did not move; (2) filed as a missing link between a match arm's reach
# literal and the discriminant — the reach literals are in the pc, and splitting
# the amount on its own gate (measured 2026-08-13) refuted nothing because the
# break is upstream, in the snapshot round-trip.
shape_area m_shape_grow
shape_area m_shape_translate
