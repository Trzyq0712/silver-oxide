domain List[T] {
    function nil(): List[T]
    function cons(x: T, xs: List[T]): List[T]

    function len(xs: List[T]): Int

    axiom {
        len(nil()) == 0
    }
}

method client() {
    assert len((nil(): List[Int])) == 0
}
